spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: true

logging:
  level:
    root: WARN
    com.michael.poc: INFO

management:
  endpoint:
    health:
      show-details: when-authorized
